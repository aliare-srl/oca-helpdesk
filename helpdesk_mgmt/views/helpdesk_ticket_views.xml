<?xml version='1.0' encoding='utf-8' ?>
<odoo>
    <record
        id="action_helpdesk_ticket_kanban_from_dashboard"
        model="ir.actions.act_window"
    >
        <field name="name">Helpdesk Ticket</field>
        <field name="res_model">helpdesk.ticket</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">
            {'default_team_id': active_id}
        </field>
        <field name="domain">[('team_id', '=', active_id)]</field>
    </record>
    <record id="helpdesk_ticket_view_search" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.search</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <search>
                <field
                    string="Ticket"
                    name="number"
                    filter_domain="['|',('number', 'ilike', self),('name', 'ilike', self)]"
                />
                <field name="partner_id" />
                <field name="user_id" />
                <field name="name" />
                <field name="tag_ids" />
                <field name="stage_id" />
                <filter
                    string="Unassigned"
                    name="unassigned"
                    domain="[('user_id','=',False)]"
                />
                <filter string="Open" name="open" domain="[('closed', '=', False )]" />
                <filter
                    string="Unattended"
                    name="unattended"
                    domain="[('unattended', '=', True )]"
                />
                <filter
                    string="High Priority"
                    name="high_priority"
                    domain="[('priority','=','3')]"
                />
                <separator />
                <filter
                    string="Archived"
                    name="archived"
                    domain="[('active','=',False)]"
                />
                <separator />
                <filter
                    string="My Tickets"
                    name="mytickets"
                    domain="[('user_id','=',uid)]"
                />
                <filter
                    string="My Followed Tickets"
                    name="my_followed_tickets"
                    domain="[('message_is_follower', '=', True)]"
                />
                <separator />
                <filter
                    name="last_week"
                    string="Last Week"
                    domain="[('create_date','&gt;', (context_today() - datetime.timedelta(weeks=1)).strftime('%%Y-%%m-%%d') )]"
                />
                <separator />
                <filter
                    string="My Activities"
                    name="activities_my"
                    domain="[('activity_ids.user_id', '=', uid)]"
                />
                <filter
                    string="Late Activities"
                    name="activities_overdue"
                    domain="[('activity_ids.date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                    help="Show all records which has next action date is before today"
                />
                <filter
                    string="Today Activities"
                    name="activities_today"
                    domain="[('activity_ids.date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"
                />
                <filter
                    string="Future Activities"
                    name="activities_upcoming_all"
                    domain="[('activity_ids.date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))
                  ]"
                />
                <separator />
                <filter
                    string="🔴 Tickets Vencidos"
                    name="sla_red"
                    domain="[('sla_status', '=', 'red')]"
                />
                <filter
                    string="🟡 Próximos a Vencer"
                    name="sla_yellow"
                    domain="[('sla_status', '=', 'yellow')]"
                />
                <filter
                    string="🟢 Tickets en Tiempo"
                    name="sla_green"
                    domain="[('sla_status', '=', 'green')]"
                />
                <group expand="0" string="Group By">
                    <filter
                        string="Partner"
                        name="group_partner"
                        domain="[]"
                        context="{'group_by':'partner_id'}"
                    />
                    <filter
                        string="Team"
                        name="group_team"
                        domain="[]"
                        context="{'group_by':'team_id'}"
                    />
                    <filter
                        string="User"
                        name="group_user"
                        domain="[]"
                        context="{'group_by':'user_id'}"
                    />
                    <filter
                        string="Stage"
                        name="group_stage"
                        domain="[]"
                        context="{'group_by':'stage_id'}"
                    />
                    <filter
                        string="Tag"
                        name="group_tag"
                        domain="[]"
                        context="{'group_by':'tag_ids'}"
                    />
                    <filter
                        string="Estado SLA"
                        name="group_sla_status"
                        domain="[]"
                        context="{'group_by':'sla_status'}"
                    />
                </group>
            </search>
        </field>
    </record>
    <record id="ticket_view_form" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <form string="Helpdesk Ticket">
                <header>
                    <button
                        string="Assign to me"
                        name="assign_to_me"
                        type="object"
                        attrs="{'invisible' : [('user_id', '!=', False)]}"
                    />
                    <field
                        name="stage_id"
                        widget="statusbar"
                        options="{'clickable': '1', 'fold_field': 'fold'}"
                    />
                </header>
                <sheet>
                    <widget
                        name="web_ribbon"
                        title="Archived"
                        bg_color="bg-danger"
                        attrs="{'invisible': [('active', '=', True)]}"
                    />
                    <div class="oe_button_box" name="button_box">
                    </div>
                    <div class="oe_title">
                        <h1 class="oe_title">
                            <field name="number" />
                        </h1>
                        <h2 class="o_row">
                            <field name="name" default_focus="1" />
                        </h2>
                    </div>
                    <group name="main">
                        <group>
                            <field name="active" invisible="1" />
                            <field name="team_id" options='{"always_reload": True}' />
                            <field name="user_ids" invisible="1" readonly="1" />
                            <field name="user_id" options='{"always_reload": True}' />
                            <field name="priority" widget="priority" />
                            <field
                                name="company_id"
                                options="{'no_create': True}"
                                groups="base.group_multi_company"
                            />
                            <field name="create_date" readonly="1" />
                            <field name="channel_id" />
                        </group>
                        <group>
                            <field name="partner_id" />
                            <field name="partner_name" />
                            <field name="partner_email" />
                            <field name="category_id" />
                            <field name="planned_date" />
                            <field
                                name="fecha_limite"
                                readonly="1"
                                attrs="{'invisible': [('fecha_limite', '=', False)]}"
                            />
                            <field
                                name="sla_status"
                                readonly="1"
                                attrs="{'invisible': [('sla_status', '=', False)]}"
                            />
                            <field
                                name="tag_ids"
                                widget="many2many_tags"
                                options="{'no_create_edit': True, 'color_field': 'color',}"
                            />
                            <field name="sequence" groups="base.group_no_one" />
                        </group>
                    </group>
                    <group>
                        <notebook>
                            <page string="Description" name="description">
                                <field name="description" widget="html" />
                            </page>
                            <page string="Other Information" name="other_info">
                                <group>
                                    <field name="last_stage_update" readonly="1" />
                                    <field name="assigned_date" readonly="1" />
                                    <field name="closed_date" readonly="1" />
                                </group>
                            </page>
                            <page string="Adjuntos" name="attachments_tab">
                                <field name="attachment_ids" widget="many2many_binary" string="Subir archivos"/>
                                <field name="attachment_ids" nolabel="1" readonly="1" mode="kanban">
                                    <kanban>
                                        <field name="id"/>
                                        <field name="mimetype"/>
                                        <field name="name"/>
                                        <templates>
                                            <t t-name="kanban-box">
                                                <div class="oe_kanban_global_click">
                                                    <div class="o_kanban_image">
                                                        <img t-if="record.mimetype.value.indexOf('image') !== -1"
                                                             t-att-src="kanban_image('ir.attachment', 'datas', record.id.raw_value)"
                                                             class="o_image_64_max"
                                                             t-att-alt="record.name.value"/>
                                                        <div t-else="" class="o_image" t-att-data-mimetype="record.mimetype.value"/>
                                                    </div>
                                                    <div class="oe_kanban_details">
                                                        <strong><field name="name"/></strong>
                                                    </div>
                                                </div>
                                            </t>
                                        </templates>
                                    </kanban>
                                </field>
                            </page>
                        </notebook>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="activity_ids" widget="mail_activity" />
                    <field
                        name="message_ids"
                        widget="mail_thread"
                        options="{'post_refresh': 'recipients', 'open_attachments': True}"
                    />
                </div>
            </form>
        </field>
    </record>
    <record id="ticket_view_tree" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.tree</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <tree
                class="o_helpdesk_aliare"
                editable="bottom"
                default_order="create_date desc"
                decoration-muted="stage_name == 'Nuevo'"
                decoration-info="stage_name == 'En progreso'"
                decoration-warning="stage_name == 'En espera'"
                decoration-success="stage_name == 'Hecho'"
                decoration-danger="stage_name == 'Cancelado'"
                decoration-primary="stage_name == 'Rechazado'"
            >
                <field name="name" default_focus="1" />
                <field name="priority" widget="priority" />
                <field name="sequence" widget="handle" />
                <field name="number" decoration-bf="1" readonly="1" />
                <field name="description" optional="hide" />
                <field name="partner_id" string="Cliente" widget="many2one" />
                <field name="partner_name" />
                <field name="team_id" />
                <field name="user_ids" invisible="1" />
                <field name="category_id" />
                <field name="channel_id" />
                <field name="user_id" widget="many2one_avatar_user" />
                <field name="planned_date" />
                <field name="stage_name" invisible="1" />
                <field name="stage_css_class" invisible="1" />
                <field name="kanban_state" invisible="1" />
                <field name="unattended" invisible="1" />
                <field name="closed" invisible="1" />
                <field name="fecha_limite" optional="show" />
                <field
                    name="sla_status"
                    optional="show"
                    decoration-success="sla_status == 'green'"
                    decoration-warning="sla_status == 'yellow'"
                    decoration-danger="sla_status == 'red'"
                    decoration-info="sla_status == 'done'"
                    widget="badge"
                />
                <field
                    name="stage_id"
                    options="{'no_create': True, 'no_open': True}"
                />
                <button
                    name="get_formview_action"
                    type="object"
                    icon="fa-external-link"
                    title="Abrir Formulario"
                />
            </tree>
        </field>
    </record>
    <record id="view_helpdesk_ticket_kanban" model="ir.ui.view">
        <field name="name">helpdesk.ticket.kanban</field>
        <field name="model">helpdesk.ticket</field>
        <field name="priority" eval="100" />
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="stage_id">
                <field name="number" />
                <field name="active" />
                <field name="name" />
                <field name="partner_name" />
                <field name="user_id" />
                <field name="sequence" />
                <field name="color" />
                <field name="stage_id" />
                <field name="priority" widget="priority" />
                <field name="assigned_date" />
                <field name="sla_status" />
                <field name="fecha_limite" />
                <progressbar
                    field="activity_state"
                    colors="{&quot;planned&quot;: &quot;success&quot;, &quot;today&quot;: &quot;warning&quot;, &quot;overdue&quot;: &quot;danger&quot;}"
                />
                <templates>
                    <t t-name="kanban-box">
                        <div
                            t-attf-class="oe_kanban_color_#{kanban_getcolor(record.color.raw_value)} oe_kanban_content oe_kanban_global_click"
                        >
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <field name="name" />
                                    <field
                                        name="tag_ids"
                                        widget="many2many_tags"
                                        options="{'color_field': 'color'}"
                                    />
                                    <field name="number" />
                                    <small class="o_kanban_record_subtitle text-muted">
                                        <field name="partner_id" />
                                    </small>
                                </div>
                                <div
                                    class="o_dropdown_kanban dropdown"
                                    t-if="!selection_mode"
                                    groups="base.group_user"
                                >
                                    <a
                                        role="button"
                                        class="dropdown-toggle o-no-caret btn"
                                        data-toggle="dropdown"
                                        data-display="static"
                                        href="#"
                                        aria-label="Dropdown menu"
                                        title="Dropdown menu"
                                    >
                                        <span class="fa fa-ellipsis-v" />
                                    </a>
                                    <div class="dropdown-menu" role="menu">
                                        <a
                                            t-if="widget.editable"
                                            role="menuitem"
                                            type="edit"
                                            class="dropdown-item"
                                        >Edit</a>
                                        <a
                                            t-if="widget.deletable"
                                            role="menuitem"
                                            type="delete"
                                            class="dropdown-item"
                                        >Delete</a>
                                        <div
                                            role="separator"
                                            class="dropdown-divider"
                                        />
                                        <ul
                                            class="oe_kanban_colorpicker"
                                            data-field="color"
                                        />
                                    </div>
                                </div>
                            </div>
                            <div class="o_kanban_record_body" />
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <field name="priority" widget="priority" />
                                    <field
                                        name="activity_ids"
                                        widget="kanban_activity"
                                        related="activity_state"
                                    />
                                    <span
                                        t-if="record.sla_status.raw_value"
                                        t-attf-class="badge badge-pill ml-1 #{record.sla_status.raw_value == 'green' and 'badge-success' or record.sla_status.raw_value == 'yellow' and 'badge-warning' or record.sla_status.raw_value == 'done' and 'badge-info' or 'badge-danger'}"
                                        t-att-title="record.sla_status.value"
                                    >
                                        <t t-esc="record.sla_status.value" />
                                    </span>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <field
                                        name="kanban_state"
                                        widget="state_selection"
                                    />
                                    <field name="activity_state" invisible="1" />
                                    <img
                                        t-att-src="kanban_image('res.users', 'image_128', record.user_id.raw_value)"
                                        t-att-title="record.user_id.value"
                                        width="24"
                                        height="24"
                                        class="oe_kanban_avatar o_image_24_cover float-right"
                                        t-att-alt="record.user_id.value"
                                    />
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="helpdesk_ticket_views_pivot" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.pivot</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <pivot string="Pivot Analysis" disable_linking="True">
                <field name="team_id" type="row" />
                <field name="assigned_date" type="col" />
            </pivot>
        </field>
    </record>
    <record id="helpdesk_ticket_view_calendar" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.calendar</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <calendar
                string="Tickets"
                date_start="planned_date"
                color="stage_color_index"
                mode="month"
            >
                <field name="name" />
                <field name="partner_id" />
                <field name="stage_id" />
            </calendar>
        </field>
    </record>
    <record id="action_duplicate_ticket" model="ir.actions.server">
        <field name="name">Duplicate</field>
        <field name="model_id" ref="model_helpdesk_ticket" />
        <field name="binding_model_id" ref="helpdesk_mgmt.model_helpdesk_ticket" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            action = model.action_duplicate_tickets()
        </field>
    </record>
    <record id="model_helpdesk_ticket_action_share" model="ir.actions.server">
        <field name="name">Share</field>
        <field name="model_id" ref="helpdesk_mgmt.model_helpdesk_ticket" />
        <field name="binding_model_id" ref="helpdesk_mgmt.model_helpdesk_ticket" />
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = records.action_share()</field>
    </record>
</odoo>
